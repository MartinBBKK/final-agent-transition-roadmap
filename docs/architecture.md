# Architecture

## Goal

Describe the system architecture for the Frontend Repo Agent.

## Initial Scope

- `apps/web`: Vue frontend for chat, review, and workflow visibility
- `apps/server`: Node.js backend for model calls, tool execution, and retrieval
- `packages/shared`: shared types across frontend and backend
- `fixtures/vue-project-sample`: sample Vue repository for indexing and tool testing

## Next Update

Add request flow, tool boundary, and RAG pipeline diagrams after the chat API is implemented.

